name: app_links
description: iOS app_links - pure ObjC patch for iOS 26.
version: 6.4.1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

flutter:
  plugin:
    platforms:
      ios:
        pluginClass: AppLinksIosPlugin

dependencies:
  flutter:
    sdk: flutter
  app_links_platform_interface: ^2.0.2
